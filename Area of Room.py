# A Python Program that ask the user to enter the width and length of a room.
width = float(input("please enter the height:"))
length = float(input("please enter the length:"))
area = width*length
print("The area is", area)

